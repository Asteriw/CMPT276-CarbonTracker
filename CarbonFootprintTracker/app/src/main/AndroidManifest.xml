<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cmpt276.kenneyw.carbonfootprinttracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".WelcomeScreen"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMenu"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".SelectCar" />
        <activity android:name=".CarbonFootPrint" />
        <activity android:name=".AddCar" />
        <activity android:name=".SelectJourney" />
        <activity android:name=".SelectRoute" />
        <activity android:name=".AddRoute" />
        <activity android:name=".EditDate"></activity>
    </application>

</manifest>